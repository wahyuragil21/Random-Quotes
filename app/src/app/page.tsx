import React from 'react';
import { Card } from 'antd';
import CardComponent from '@/components/card';

const { Meta } = Card;

const Home = () => (
  <div className="App">
    <CardComponent />
  </div>
);

export default Home;