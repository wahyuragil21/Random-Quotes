import React from 'react';
import { Skeleton } from 'antd';

const SkeletonComponent = () => <Skeleton active style={{ textAlign: 'center', justifyItems: 'center' }} />;

export default SkeletonComponent;